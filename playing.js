// {sourcenode:{targetnode: node, weight: n}, sourcenode2: {targetnode: weight}}
//input paths    {uid:[[exercise, score],[]], uid2:[[],[],[]]}

module.exports = {
  pathsToGraph: function(paths){
    var graph = {}

    return graph
  }
}


newPaths = []
for uid in paths:
  for a,b in paths[uid]:

{}

for every path in the object
  for node, nextnode in the path
    graph[node] = {nextnode[0]: nextnode[1]} //get ex name and weight of target node and attach to source node

arr = [[A:6],[],[],[],[F:9]]

for(i=0, i < arr.length-1, i++){

}



// def parse_txt_file(graph_info) "AB8, CD8"
//    graph_info.scan(/\w\w\d+/).each do |x|
//      start = x[0,1]
//      finish = x[1,1]
//      @graph[start] ||= { }
//      @graph[start][finish] = x[2..-1].to_i
//      p @graph
//    end
//  end
